-- V14: откат V13.
--
-- После одного цикла прода решили: цену позиции вручную не редактируем,
-- она = сумма услуг. Манульной правкой остаются только цены услуг (там есть
-- свой is_manual_price на order_item_services). На уровне позиции флаг
-- больше не нужен — дропаем колонку.

ALTER TABLE order_items
    DROP COLUMN IF EXISTS is_manual_price;
