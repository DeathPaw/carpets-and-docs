-- V26: модификатор «Многодетная семья» -10%.
--
-- По просьбе оператора: та же механика что «Пенсионер»/«Постоянный клиент» —
-- оператор ставит его на карточку клиента, при создании нового заказа
-- OrderService.copyFromClient автоматически подтягивает модификатор в заказ.
-- Специального флага в clients не заводим (лишняя дубль-информация).

INSERT INTO price_modifiers (name, percent) VALUES
  ('Многодетная семья', -10.00)
ON CONFLICT (name) DO NOTHING;
