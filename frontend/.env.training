# Активируется при `vite --mode training` (см. npm run dev:training).
# Фронт читает этот флаг через import.meta.env.VITE_TRAINING — показывает баннер
# тренажёра и автоматически запускает интерактивный тур.
VITE_TRAINING=1
